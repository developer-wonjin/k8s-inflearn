# 2-1) Pod — 라벨 6개 조합

원문 : https://cafe.naver.com/kubeops/497 (2. Label)

`type`(web/db/server) × `lo`(dev/production) 조합으로 Pod 6개를 만들어
라벨 셀렉터가 어떻게 골라내는지 확인한다.

| Pod | type | lo |
|---|---|---|
| pod-1 | web | dev |
| pod-2 | db | dev |
| pod-3 | server | dev |
| pod-4 | web | production |
| pod-5 | db | production |
| pod-6 | server | production |

## 실습 시작 전 정리

```bash
clear                       # 화면 정리 후 시작

# default 네임스페이스의 실습 리소스를 전부 삭제 (시스템 네임스페이스는 건드리지 않는다)
kubectl delete pod,deploy,rs,statefulset,daemonset,job,cronjob \
  --all -n default --grace-period=1 --ignore-not-found

# Service는 kubernetes(클러스터 기본 서비스)만 남기고 삭제
kubectl delete svc -n default \
  --field-selector 'metadata.name!=kubernetes' --ignore-not-found

kubectl get all -n default  # service/kubernetes 만 남으면 정상
```

> **`-n default`를 반드시 붙인다.** `-A`(전 네임스페이스)로 실행하면 `kube-system`,
> `calico-system` 등 클러스터 구동에 필요한 리소스까지 지워져 **클러스터가 망가진다.**
>
> `service/kubernetes`는 API 서버로 가는 기본 서비스라 `--field-selector`로 제외한다.
> 지워도 자동 재생성되지만 그 사이 통신이 끊긴다.

## YAML

```yaml
apiVersion: v1                 # Pod은 core 그룹
kind: Pod
metadata:
  name: pod-1                  # Pod 이름
  labels:                      # 라벨은 key:value 형태로 여러 개 붙일 수 있다
    type: web                  # 용도 구분용 라벨
    lo: dev                    # 환경(location) 구분용 라벨
spec:
  containers:
  - name: container            # 컨테이너 이름
    image: kubetm/init         # 계속 떠 있기만 하는 실습용 이미지
---                            # 한 파일에 여러 리소스를 담는 구분자
apiVersion: v1
kind: Pod
metadata:
  name: pod-2
  labels:
    type: db                   # pod-1과 type만 다름
    lo: dev
spec:
  containers:
  - name: container
    image: kubetm/init
---
apiVersion: v1
kind: Pod
metadata:
  name: pod-3
  labels:
    type: server
    lo: dev
spec:
  containers:
  - name: container
    image: kubetm/init
---
apiVersion: v1
kind: Pod
metadata:
  name: pod-4
  labels:
    type: web                  # pod-1과 type은 같고 lo만 다름
    lo: production
spec:
  containers:
  - name: container
    image: kubetm/init
---
apiVersion: v1
kind: Pod
metadata:
  name: pod-5
  labels:
    type: db
    lo: production
spec:
  containers:
  - name: container
    image: kubetm/init
---
apiVersion: v1
kind: Pod
metadata:
  name: pod-6
  labels:
    type: server
    lo: production
spec:
  containers:
  - name: container
    image: kubetm/init
```

## 실습 과정

### 1. 생성

앞 섹션(1. Container)의 `pod-1`, `deployment-1`을 먼저 지워야 이름이 겹치지 않는다.

```bash
clear                                                         # 화면 정리 후 시작
kubectl delete pod pod-1 --grace-period=1 --ignore-not-found  # 1-1의 pod-1 제거 (없어도 에러 안 남)
kubectl delete deploy deployment-1 --ignore-not-found         # 1-2의 deployment-1 제거

kubectl apply -f - <<'END'                                    # Pod 6개를 한 번에 생성 (--- 로 리소스 구분) — YAML 설명은 위 YAML 절 참고
# ... 위 YAML 전체 ...
END

# Pod 6개가 전부 Ready 될 때까지 대기 (최대 120초)
kubectl wait --for=condition=Ready \
  pod/pod-1 \
  pod/pod-2 \
  pod/pod-3 \
  pod/pod-4 \
  pod/pod-5 \
  pod/pod-6 \
  --timeout=120s
```

```
pod/pod-1 created
pod/pod-2 created
pod/pod-3 created
pod/pod-4 created
pod/pod-5 created
pod/pod-6 created
pod/pod-1 condition met
... (6개 모두 condition met)
```

### 2. 붙은 라벨 확인

```bash
clear                          # 화면 정리 후 시작
kubectl get pod --show-labels  # LABELS 열을 추가해 붙은 라벨을 그대로 확인
```

```
NAME    READY   STATUS    AGE   LABELS
pod-1   1/1     Running   14s   lo=dev,type=web
pod-2   1/1     Running   14s   lo=dev,type=db
pod-3   1/1     Running   14s   lo=dev,type=server
pod-4   1/1     Running   14s   lo=production,type=web
pod-5   1/1     Running   14s   lo=production,type=db
pod-6   1/1     Running   14s   lo=production,type=server
```

### 3. 라벨 셀렉터로 조회

```bash
clear                                      # 화면 정리 후 시작
kubectl get pod -l type=web                # 단일 조건 — type이 web인 Pod
kubectl get pod -l lo=production           # 단일 조건 — lo가 production인 Pod
kubectl get pod -l type=web,lo=production  # 쉼표는 OR가 아니라 AND (둘 다 만족)
kubectl get pod -l 'type in (web,db)'      # 집합 조건 — type이 web 또는 db
```

| 셀렉터 | 결과 |
|---|---|
| `type=web` | pod-1, pod-4 |
| `lo=production` | pod-4, pod-5, pod-6 |
| `type=web,lo=production` | pod-4 |
| `type in (web,db)` | pod-1, pod-2, pod-4, pod-5 |

- 쉼표는 **OR가 아니라 AND**다. 두 라벨을 모두 만족하는 Pod만 나온다.
- `in (...)`, `notin (...)`, `!key` 같은 집합 기반 조건도 쓸 수 있다.

### 4. Pod IP 대조표 (다음 문서에서 Endpoint와 맞춰볼 값)

```bash
clear  # 화면 정리 후 시작
# Pod 이름·IP·라벨 두 개를 한 표로 뽑아 다음 문서(Endpoints)와 대조할 준비를 한다
kubectl get pod -o custom-columns='NAME:.metadata.name,IP:.status.podIP,TYPE:.metadata.labels.type,LO:.metadata.labels.lo'
```

```
NAME    IP              TYPE     LO
pod-1   20.110.126.26   web      dev
pod-2   20.100.194.94   db       dev
pod-3   20.100.194.93   server   dev
pod-4   20.110.126.27   web      production
pod-5   20.100.194.95   db       production
pod-6   20.110.126.28   server   production
```

### 정리

Service까지 실습한 뒤 [2-2.service-label.md](2-2.service-label.md)의 정리 명령을 실행한다.

## 배운 것

- 라벨은 `key: value` 형태로 한 리소스에 여러 개 붙는다.
- 용도는 두 가지 — **정보 표시**, 그리고 **리소스 간 연결**(Service → Pod).
- Pod뿐 아니라 모든 리소스에 붙일 수 있다.
- `-l`의 쉼표는 AND다.
